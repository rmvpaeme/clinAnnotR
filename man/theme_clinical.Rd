% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_clinical}
\alias{theme_clinical}
\title{Clinical figure theme}
\usage{
theme_clinical(
  show_x = FALSE,
  legend_pos = "right",
  base_size = 9,
  grid_color = "#D8DEE9",
  muted_color = "#4C566A",
  axis_title_size = base_size - 1,
  legend_text_size = base_size - 1,
  legend_key_size = 0.65,
  plot_margin = c(3, 5, 3, 5)
)
}
\arguments{
\item{show_x}{Logical. Show x-axis text, title, and ticks? Set \code{TRUE} on the
bottom-most panel. Default: \code{FALSE}.}

\item{legend_pos}{Legend position passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}. Default: \code{"right"}.}

\item{base_size}{Numeric. Base font size (pt). Default: \code{9}.}

\item{grid_color}{Hex colour for major grid lines. Default: \code{"#D8DEE9"}.}

\item{muted_color}{Hex colour for axis decorations (logticks, etc.). Default: \code{"#4C566A"}.}

\item{axis_title_size}{Numeric. y-axis title font size. Default: \code{base_size - 1}.}

\item{legend_text_size}{Numeric. Legend text font size. Default: \code{base_size - 1}.}

\item{legend_key_size}{Numeric. Legend key height in \code{"lines"} units. Default: \code{0.65}.}

\item{plot_margin}{Numeric vector \code{c(top, right, bottom, left)} in pt.
Default: \code{c(3, 5, 3, 5)}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object (additive).
}
\description{
A \code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}-based theme tuned for stacked multi-panel clinical
figures. Minor grid lines and superfluous decorations are removed; major grid
lines are retained in a soft colour.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_clinical(show_x = TRUE)

}
