% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_gantt.R
\name{make_gantt_panel}
\alias{make_gantt_panel}
\title{Build a treatment Gantt panel for one case}
\usage{
make_gantt_panel(
  treatment_data,
  case_label = NULL,
  highlight_days = NULL,
  x_range = NULL,
  show_x = FALSE,
  bar_height = 0.35,
  bar_alpha = 0.85,
  label_size = 3,
  label_nudge_x = -1,
  x_label = "Days from diagnosis",
  x_breaks_step = 50,
  group_by_class = TRUE,
  show_class_labels = TRUE,
  class_label_size = 2.5,
  class_label_margin = 60,
  nord = NULL,
  base_size = 9,
  title_size = 9,
  vline_linetype = "dashed",
  vline_linewidth = 0.4,
  vline_alpha = 0.5
)
}
\arguments{
\item{treatment_data}{A data frame produced by \code{\link[=load_treatment_data]{load_treatment_data()}} or
\code{\link[=prep_treatment_data]{prep_treatment_data()}}, filtered to \strong{one case} (i.e. a single unique
\code{case_id}). Columns required: \code{TREATMENT}, \code{START_rel}, \code{END_rel},
\code{COLOR}. Column \code{CLASS} is required when \code{group_by_class = TRUE}.}

\item{case_label}{Character. Panel title shown above the chart (e.g.
\code{"Case 1 — treatments"}). Default: \code{NULL} (no title).}

\item{highlight_days}{Named numeric vector of reference line positions
(dashed vertical lines). \code{NULL} suppresses lines. Default: \code{NULL}.}

\item{x_range}{Numeric length-2. x-axis limits in days. \code{NULL} = auto
(data range with 5-day padding). Default: \code{NULL}.}

\item{show_x}{Logical. Show x-axis text, title, and ticks? Default: \code{FALSE}.}

\item{bar_height}{Numeric. Half-height of each bar on the y-axis (bars span
\code{y - bar_height} to \code{y + bar_height}). Default: \code{0.35}.}

\item{bar_alpha}{Numeric. Bar fill alpha. Default: \code{0.85}.}

\item{label_size}{Numeric. Text size for drug name labels. Default: \code{3}.}

\item{label_nudge_x}{Numeric. Horizontal offset (in days) applied to drug
name labels relative to \code{first_start}. Negative values push labels to the
left. Default: \code{-1}.}

\item{x_label}{Character. x-axis title (shown only when \code{show_x = TRUE}).
Default: \code{"Days from diagnosis"}.}

\item{x_breaks_step}{Numeric. Step size for automatic x-axis break positions.
Default: \code{50}.}

\item{group_by_class}{Logical. Sort treatments by drug class (ordered by the
earliest start in that class), draw separator lines between classes, and
add class labels to the right? Default: \code{TRUE}.}

\item{show_class_labels}{Logical. Draw class name labels to the right of
each class group when \code{group_by_class = TRUE}. Default: \code{TRUE}.}

\item{class_label_size}{Numeric. Text size for class labels. Default: \code{2.5}.}

\item{class_label_margin}{Numeric. Extra right margin (in pt) added to the
panel when class labels are shown, to prevent them being clipped by the
figure boundary. Default: \code{60}.}

\item{nord}{Named list with \verb{$dark}, \verb{$muted}, \verb{$grid} (hex colours).
Default: Nord palette.}

\item{base_size}{Numeric. Base font size. Default: \code{9}.}

\item{title_size}{Numeric. Font size for the panel title. Default: \code{9}.}

\item{vline_linetype}{Character. Linetype for reference lines. Default:
\code{"dashed"}.}

\item{vline_linewidth}{Numeric. Width of reference lines. Default: \code{0.4}.}

\item{vline_alpha}{Numeric. Alpha of reference lines. Default: \code{0.5}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Creates a horizontal Gantt chart showing drug treatment bars for a single
case. Bars are coloured by the \code{COLOR} column in the data (user-controlled
per row), allowing full control over class-level colour coding without
requiring a legend. Drug name labels are placed to the left of each bar's
first segment.
}
\details{
When \code{group_by_class = TRUE} (default), treatments are sorted by drug class
(ordered by the earliest treatment start within each class) and a thin
separator line is drawn between classes. A class label is placed to the
right of each group.

Duplicate segments (identical \code{TREATMENT}, \code{START_rel}, and \code{END_rel}) are
silently removed before plotting.
}
\examples{
tx <- example_data("treatment")
make_gantt_panel(
  tx[tx$case_id == "Case 1", ],
  case_label     = "Case 1 — treatments",
  highlight_days = c("D1" = 1, "D22" = 22, "D49" = 49),
  x_range        = c(-5, 190),
  show_x         = TRUE
)

}
