% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assemble.R
\name{make_clinical_figure}
\alias{make_clinical_figure}
\title{Assemble a full multi-panel clinical figure}
\usage{
make_clinical_figure(
  lab_data,
  treatment_data = NULL,
  lab_panels,
  cases = NULL,
  case_palette = NULL,
  case_shapes = NULL,
  x_range = NULL,
  highlight_days = NULL,
  gantt_height_weight = NULL,
  caption = NULL,
  caption_size = 7,
  nord = NULL,
  base_size = 9
)
}
\arguments{
\item{lab_data}{A data frame produced by \code{\link[=load_lab_data]{load_lab_data()}} or
\code{\link[=prep_lab_data]{prep_lab_data()}}.}

\item{treatment_data}{A data frame produced by \code{\link[=load_treatment_data]{load_treatment_data()}} or
\code{\link[=prep_treatment_data]{prep_treatment_data()}}. Pass \code{NULL} to omit all Gantt panels.}

\item{lab_panels}{A list of \code{lab_panel_spec} objects created with
\code{\link[=lab_panel]{lab_panel()}}. At least one element is required.}

\item{cases}{Character vector of case IDs to include and the order in which
their Gantt panels are stacked. \code{NULL} = all unique \code{case_id} values in
\code{lab_data}, in the order they first appear. Default: \code{NULL}.}

\item{case_palette}{Named character vector mapping case IDs to hex colours.
\code{NULL} = auto-assigned from the built-in Nord-inspired palette.
Default: \code{NULL}.}

\item{case_shapes}{Named integer vector mapping case IDs to ggplot2 shape
codes used in the \strong{Case} legend key. \code{NULL} = auto-assigned.
Default: \code{NULL}.}

\item{x_range}{Numeric length-2. Shared x-axis limits in days applied to
every panel. \code{NULL} = auto-computed as \code{c(min(relday) - 5, max(relday) + 5)} across the entire \code{lab_data}. Default: \code{NULL}.}

\item{highlight_days}{Named numeric vector of reference line positions shown
on \strong{every} panel (e.g. \code{c("D1" = 1, "D22" = 22, "D49" = 49)}).
Individual lab panels can override this via \code{\link[=lab_panel]{lab_panel()}}'s
\code{highlight_days} argument. \code{NULL} = no reference lines. Default: \code{NULL}.}

\item{gantt_height_weight}{Positive numeric or \code{NULL}. Relative height of
each Gantt panel in the patchwork stack. When \code{NULL} (default), the height
is computed automatically as \code{max(1.5, n_treatments * 0.4)} so panels with
many treatment rows are taller. Supply a fixed number to override for all
Gantt panels. Default: \code{NULL}.}

\item{caption}{Character. Caption shown below the figure. \code{NULL} = a
built-in default mentioning protocol timepoints and BDL. Default: \code{NULL}.}

\item{caption_size}{Numeric. Caption font size. Default: \code{7}.}

\item{nord}{Named list with \verb{$dark}, \verb{$muted}, \verb{$grid} (hex colours).
\code{NULL} = built-in Nord palette. Default: \code{NULL}.}

\item{base_size}{Numeric. Base font size applied to all panels. Default: \code{9}.}
}
\value{
A \link[patchwork:patchwork-package]{patchwork} object. Print it to
display, or pass it to \code{\link[=save_clinical_figure]{save_clinical_figure()}}.
}
\description{
Builds a stacked figure by combining one or more lab time-series panels
(defined by \code{\link[=lab_panel]{lab_panel()}} specifications) with one Gantt chart panel per
case. All panels share a common x-axis range; the x-axis label and tick
marks are shown only on the bottom-most panel.
}
\examples{
lab <- example_data("lab")
tx  <- example_data("treatment")

panels <- list(
  lab_panel(
    line_params   = "WBC (/µL)",
    point_params  = "peripheral blasts (/µL)",
    y_label       = "Count (/µL)",
    height_weight = 4
  ),
  lab_panel(
    point_params  = "BM blasts with IF (\%)",
    show_labels   = TRUE,
    label_suffix  = "\%",
    y_limits      = c(0.05, 200),
    y_breaks      = c(0.1, 1, 5, 20, 100),
    y_labels      = paste0(c(0.1, 1, 5, 20, 100), "\%"),
    y_label       = "BM blasts (\%)",
    height_weight = 3
  )
)

fig <- make_clinical_figure(
  lab_data       = lab,
  treatment_data = tx,
  lab_panels     = panels,
  highlight_days = c("D1" = 1, "D22" = 22, "D49" = 49)
)
\dontrun{
print(fig)
}

}
