% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{load_lab_data}
\alias{load_lab_data}
\title{Load and pre-process lab data from an Excel file}
\usage{
load_lab_data(
  path,
  cases,
  sheet = 1,
  col_patientid = "patientID",
  col_parameter = "parameter",
  col_value = "value",
  col_date = "date",
  col_time = "time",
  bdl_floor = 0.1,
  label_digits = 1L,
  tz = "UTC"
)
}
\arguments{
\item{path}{Character. Path to the Excel file.}

\item{cases}{A list of case specifications. Each element must be a named list
with:
\itemize{
\item \code{id} — character, must match the patient ID column in the file.
\item \code{ref_date} — character in \code{"YYYY-MM-DD"} format, used as day 0.
}

Example: \code{list(list(id = "Case 1", ref_date = "2025-06-03"))}.}

\item{sheet}{Sheet name or index passed to \code{\link[readxl:read_excel]{readxl::read_excel()}}. Default: \code{1}.}

\item{col_patientid}{Column name for the patient/case identifier. Default:
\code{"patientID"}.}

\item{col_parameter}{Column name for the measurement parameter label. Default:
\code{"parameter"}.}

\item{col_value}{Column name for the raw value. May be character (supports
\code{"<X"} BDL notation) or numeric. Default: \code{"value"}.}

\item{col_date}{Column name for the date of measurement (Excel date or
date-time). Default: \code{"date"}.}

\item{col_time}{Column name for the time of measurement (Excel time).
Pass \code{NULL} if time information is embedded in \code{col_date} or not available;
midnight (00:00:00) is then assumed. Default: \code{"time"}.}

\item{bdl_floor}{Numeric. Floor value applied to BDL and zero observations
so they can be plotted on a log10 axis. Default: \code{0.1}.}

\item{label_digits}{Integer. Rounding digits for numeric value labels shown
on plot points. Default: \code{1}.}

\item{tz}{Character. Time zone for all date-time arithmetic. Default: \code{"UTC"}.}
}
\value{
A data frame with columns:
\code{case_id}, \code{relday}, \code{parameter}, \code{value_raw}, \code{value_coerce},
\code{bdl}, \code{value_num}, \code{value_label}.
}
\description{
Reads an Excel workbook, filters to the requested cases, computes relative
days from each case's reference date, and flags below-detection-limit (BDL)
values. BDL values are those whose raw string starts with \code{"<"} or whose
numeric value is zero.
}
\examples{
\dontrun{
cases <- list(
  list(id = "Case 1", ref_date = "2025-06-03"),
  list(id = "Case 2", ref_date = "2025-08-12")
)
lab <- load_lab_data("labvals.xlsx", cases)
}

}
\seealso{
\code{\link[=prep_lab_data]{prep_lab_data()}} for data frames with pre-computed relative days.
}
