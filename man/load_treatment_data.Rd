% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{load_treatment_data}
\alias{load_treatment_data}
\title{Load and pre-process treatment data from an Excel file}
\usage{
load_treatment_data(
  path,
  cases,
  sheet = 1,
  col_patientid = "PATIENTID",
  col_treatment = "TREATMENT",
  col_start = "START",
  col_end = "END",
  col_color = "COLOR",
  col_class = "CLASS",
  tz = "UTC"
)
}
\arguments{
\item{path}{Character. Path to the Excel file.}

\item{cases}{A list of case specifications (same format as \code{\link[=load_lab_data]{load_lab_data()}}).}

\item{sheet}{Sheet name or index. Default: \code{1}.}

\item{col_patientid}{Column name for the patient/case identifier.
Default: \code{"PATIENTID"}.}

\item{col_treatment}{Column name for the drug/treatment name. Rows with the
same name for one case are rendered as a multi-segment Gantt bar.
Default: \code{"TREATMENT"}.}

\item{col_start}{Column name for the treatment start date-time (Excel
date-time). Default: \code{"START"}.}

\item{col_end}{Column name for the treatment end date-time. Default: \code{"END"}.}

\item{col_color}{Column name for the bar fill colour (hex string, e.g.
\code{"#88C0D0"}). Colour is stored per row so different segments of the same
treatment can differ. Default: \code{"COLOR"}.}

\item{col_class}{Column name for the drug class label (e.g. \code{"Anthracycline"}).
Retained for user reference; not currently used as a visual aesthetic.
Default: \code{"CLASS"}.}

\item{tz}{Character. Time zone. Default: \code{"UTC"}.}
}
\value{
A data frame with columns:
\code{case_id}, \code{TREATMENT}, \code{START_rel}, \code{END_rel}, \code{COLOR}, \code{CLASS}.
}
\description{
Reads treatment records from an Excel workbook and computes start and end
times as days relative to each case's reference date.
}
\examples{
\dontrun{
cases <- list(list(id = "Case 1", ref_date = "2025-06-03"))
tx <- load_treatment_data("treatment.xlsx", cases)
}

}
\seealso{
\code{\link[=prep_treatment_data]{prep_treatment_data()}} for data frames with pre-computed relative days.
}
