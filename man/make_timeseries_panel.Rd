% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_timeseries.R
\name{make_timeseries_panel}
\alias{make_timeseries_panel}
\title{Build a time-series panel for any numerical lab parameters}
\usage{
make_timeseries_panel(
  lab_data,
  spec,
  case_palette = NULL,
  case_shapes = NULL,
  line_linetypes = NULL,
  point_shapes_det = NULL,
  x_range = c(-5, 190),
  show_x = FALSE,
  shade_regions = NULL,
  nord = NULL,
  base_size = 9
)
}
\arguments{
\item{lab_data}{A data frame produced by \code{\link[=load_lab_data]{load_lab_data()}} or
\code{\link[=prep_lab_data]{prep_lab_data()}}, containing columns \code{case_id}, \code{relday}, \code{parameter},
\code{value_num}, \code{value_label}, and \code{bdl}. Extra columns are ignored.}

\item{spec}{A \code{lab_panel_spec} object from \code{\link[=lab_panel]{lab_panel()}}.}

\item{case_palette}{Named character vector mapping case IDs to hex colours.
Unknown case IDs are assigned colours automatically.}

\item{case_shapes}{Named integer vector mapping case IDs to shapes used in
the \strong{Case} legend key. Default shapes are filled circles/triangles.}

\item{line_linetypes}{Named character vector mapping \code{line_param} values to
linetype strings (e.g. \code{"solid"}, \code{"dashed"}). Used only in multi-case
mode; ignored when there is a single case (all lines are solid).}

\item{point_shapes_det}{Named integer vector mapping \code{point_param} values to
ggplot2 shape codes for detected (non-BDL) points. Auto-assigned if \code{NULL}.}

\item{x_range}{Numeric vector of length 2. x-axis limits in days.
Default: \code{c(-5, 190)}.}

\item{show_x}{Logical. Show x-axis text, title, and ticks? Default: \code{FALSE}.}

\item{shade_regions}{A list of background shading rectangles drawn behind
all data layers. Each element is a named list with:
\itemize{
\item \code{xmin}, \code{xmax} — start and end in relative days
\item \code{fill} — hex fill colour (e.g. \code{"#BF616A"})
\item \code{alpha} — fill transparency (default \code{0.15})
}

Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  list(xmin = 5,  xmax = 7,  fill = "#BF616A", alpha = 0.15),
  list(xmin = 19, xmax = 38, fill = "#D08770", alpha = 0.15)
)
}\if{html}{\out{</div>}}

\code{NULL} (default) draws no regions.}

\item{nord}{Named list with elements \verb{$dark}, \verb{$muted}, \verb{$grid} (hex
colours). Pass your own or leave \code{NULL} to use the package Nord palette.}

\item{base_size}{Numeric. Base font size. Default: \code{9}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Creates a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} panel showing one or more lab parameters over
time. Parameters can be rendered as connected lines, points, or both.
Below-detection-limit (BDL) values—those whose raw string starts with \code{"<"}
or whose numeric value is zero—are shown with a dedicated open-triangle
symbol regardless of the parameter type.
}
\details{
When \strong{only one case} is present in \code{lab_data}, colour is mapped to
\strong{parameter} (each parameter gets a distinct colour; all lines are solid).
When \strong{multiple cases} are present, colour is mapped to \strong{case} and
linetype distinguishes parameters (existing multi-case behaviour).
}
\examples{
\donttest{
lab <- example_data("lab")
spec <- lab_panel(
  line_params  = "WBC (/µL)",
  point_params = "peripheral blasts (/µL)",
  y_label      = "Count (/µL)"
)
make_timeseries_panel(lab, spec)
}

}
