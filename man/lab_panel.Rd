% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_spec.R
\name{lab_panel}
\alias{lab_panel}
\title{Construct a lab panel specification}
\usage{
lab_panel(
  line_params = character(0),
  point_params = character(0),
  y_scale = c("log10", "linear"),
  y_label = NULL,
  y_limits = NULL,
  y_breaks = NULL,
  y_labels = NULL,
  bdl_floor = NULL,
  show_labels = FALSE,
  label_suffix = "",
  label_size = 2.8,
  label_nudge_y = 0.18,
  label_nudge_y_bdl = -0.25,
  label_segment_size = 0.3,
  label_min_segment = 0.2,
  highlight_days = NULL,
  vline_labels = TRUE,
  line_linewidth = 0.65,
  point_size = 2.5,
  bdl_shape = 6L,
  bdl_size = 2.5,
  logticks = TRUE,
  height_weight = 3
)
}
\arguments{
\item{line_params}{Character vector. Parameter values (from \code{lab_data$parameter})
to display as connected lines. Each distinct parameter receives a unique
linetype. Default: \code{character(0)} (no lines).}

\item{point_params}{Character vector. Parameters to display as points. Each
distinct parameter receives a unique point shape. Below-detection-limit
(BDL) observations are rendered with a dedicated symbol regardless of
parameter. Default: \code{character(0)} (no points).}

\item{y_scale}{Character. \code{"log10"} for a logarithmic y-axis (recommended for
counts and percentages spanning several orders of magnitude) or \code{"linear"}
for a standard linear axis. Default: \code{"log10"}.}

\item{y_label}{Character. y-axis title. \code{NULL} joins all parameter names with
\code{" / "} as an automatic label. Default: \code{NULL}.}

\item{y_limits}{Numeric vector of length 2 \code{c(lo, hi)}. \code{NULL} = auto-computed
from the data (rounded to powers of 10 for log scale). Default: \code{NULL}.}

\item{y_breaks}{Numeric vector of break positions. \code{NULL} = default breaks
(powers of 10 for log scale; \code{ggplot2} default for linear). Default: \code{NULL}.}

\item{y_labels}{Character vector or labelling function for axis tick labels.
\code{NULL} = SI-suffixed numbers for log scale, default for linear.
Default: \code{NULL}.}

\item{bdl_floor}{Numeric. Value assigned to BDL observations for log-axis
rendering. Overrides the global \code{bdl_floor} in \code{\link[=make_clinical_figure]{make_clinical_figure()}}
for this panel only. \code{NULL} = use global default. Default: \code{NULL}.}

\item{show_labels}{Logical. Draw value labels next to each non-BDL point
using \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}? Useful for sparse panels such as bone
marrow blast percentages. Default: \code{FALSE}.}

\item{label_suffix}{Character appended to every value label (e.g. \code{"\%"} for
percentage parameters). Default: \code{""}.}

\item{label_size}{Numeric text size for value labels. Default: \code{2.8}.}

\item{label_nudge_y}{Numeric. \code{nudge_y} for detected-value labels (additive
on the log10 scale when \code{y_scale = "log10"}). Default: \code{0.18}.}

\item{label_nudge_y_bdl}{Numeric. \code{nudge_y} for BDL labels. Default: \code{-0.25}.}

\item{label_segment_size}{Numeric. Width of the ggrepel connector segment.
Default: \code{0.3}.}

\item{label_min_segment}{Numeric. Minimum segment length below which no
connector is drawn. Default: \code{0.2}.}

\item{highlight_days}{Named numeric vector of reference line positions for
this panel only. \code{NULL} = inherit the global \code{highlight_days} argument of
\code{\link[=make_clinical_figure]{make_clinical_figure()}}. Default: \code{NULL}.}

\item{vline_labels}{Logical. Show rotated day labels on reference lines?
Default: \code{TRUE}.}

\item{line_linewidth}{Numeric. Stroke width for line geometries. Default: \code{0.65}.}

\item{point_size}{Numeric. Size of detected (non-BDL) point geometries.
Default: \code{2.5}.}

\item{bdl_shape}{Integer. ggplot2 shape code for BDL points. Shape \code{6} is an
open downward-pointing triangle (∇). Default: \code{6L}.}

\item{bdl_size}{Numeric. Size of BDL points. Default: \code{2.5}.}

\item{logticks}{Logical. Annotate log10 panels with minor log ticks
on the left axis (\code{\link[ggplot2:annotation_logticks]{ggplot2::annotation_logticks()}})? Default: \code{TRUE}.}

\item{height_weight}{Positive numeric. Relative panel height in the assembled
figure (passed to \code{\link[patchwork:plot_layout]{patchwork::plot_layout()}}). Default: \code{3}.}
}
\value{
A named list of class \code{c("lab_panel_spec", "list")}.
}
\description{
Builds a specification object describing one time-series panel for use in
\code{\link[=make_clinical_figure]{make_clinical_figure()}}. Each spec controls which parameters are shown,
how the y-axis is scaled, and all visual details for that panel. The
specification is a plain named list of class \code{"lab_panel_spec"}; no plotting
occurs here.
}
\examples{
# WBC line + peripheral blast points on a shared log10 count axis
lab_panel(
  line_params  = "WBC (/µL)",
  point_params = "peripheral blasts (/µL)",
  y_label      = "Count (/µL)",
  height_weight = 4
)

# BM blast percentages with direct value labels
lab_panel(
  point_params  = "BM blasts with IF (\%)",
  show_labels   = TRUE,
  label_suffix  = "\%",
  y_limits      = c(0.05, 200),
  y_breaks      = c(0.1, 1, 5, 20, 100),
  y_labels      = paste0(c(0.1, 1, 5, 20, 100), "\%"),
  y_label       = "BM blasts (\%)",
  height_weight = 3
)

# Any linear-scale parameter
lab_panel(
  line_params  = c("Hemoglobin (g/dL)", "Albumin (g/dL)"),
  y_scale      = "linear",
  y_label      = "Concentration (g/dL)"
)

}
