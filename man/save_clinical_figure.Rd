% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_clinical_figure}
\alias{save_clinical_figure}
\title{Save a clinical figure to file}
\usage{
save_clinical_figure(
  figure,
  path,
  width = 7,
  height = 8,
  units = "in",
  dpi = 300,
  device = NULL,
  ...
)
}
\arguments{
\item{figure}{A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} or
\link[patchwork:patchwork-package]{patchwork} object, typically the output of
\code{\link[=make_clinical_figure]{make_clinical_figure()}}.}

\item{path}{Character. Output file path. The device is inferred from the
extension: \code{.pdf}, \code{.svg}, \code{.png}, \code{.tiff}, \code{.jpg}, \code{.eps} are all
supported via \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width}{Numeric. Figure width. Default: \code{7}.}

\item{height}{Numeric. Figure height. Default: \code{8}.}

\item{units}{Character. Units for \code{width} and \code{height}. One of \code{"in"},
\code{"cm"}, \code{"mm"}, \code{"px"}. Default: \code{"in"}.}

\item{dpi}{Numeric. Resolution in dots per inch (raster outputs only).
Default: \code{300}.}

\item{device}{Character or function. Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. \code{NULL}
= inferred from the file extension. Default: \code{NULL}.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly returns \code{path}.
}
\description{
A thin wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with clinically sensible defaults
and automatic device detection from the file extension.
}
\examples{
\dontrun{
lab <- example_data("lab")
tx  <- example_data("treatment")
fig <- make_clinical_figure(
  lab, tx,
  lab_panels = list(lab_panel(line_params = "WBC (/µL)"))
)
save_clinical_figure(fig, "figure.pdf")
save_clinical_figure(fig, "figure.png", dpi = 150)
save_clinical_figure(fig, "figure.svg", width = 14, height = 16)
}

}
