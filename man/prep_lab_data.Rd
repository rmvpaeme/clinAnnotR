% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{prep_lab_data}
\alias{prep_lab_data}
\title{Prepare a data frame of lab values with pre-computed relative days}
\usage{
prep_lab_data(
  data,
  col_caseid = "case_id",
  col_relday = "relday",
  col_parameter = "parameter",
  col_value = "value",
  bdl_floor = 0.1,
  label_digits = 1L
)
}
\arguments{
\item{data}{A data frame. Must contain at minimum the columns identified by
\code{col_caseid}, \code{col_relday}, \code{col_parameter}, and \code{col_value}.}

\item{col_caseid}{Column name for the case identifier. Default: \code{"case_id"}.}

\item{col_relday}{Column name for relative days from reference date.
Default: \code{"relday"}.}

\item{col_parameter}{Column name for the measurement parameter label.
Default: \code{"parameter"}.}

\item{col_value}{Column name for the raw value. May contain \code{"<X"} prefixes
to indicate below-detection-limit values. Default: \code{"value"}.}

\item{bdl_floor}{Numeric floor for BDL/zero values (for log-axis plotting).
Default: \code{0.1}.}

\item{label_digits}{Integer rounding digits for numeric value labels.
Default: \code{1}.}
}
\value{
A data frame with standardised columns: \code{case_id}, \code{relday},
\code{parameter}, \code{value_raw}, \code{value_coerce}, \code{bdl}, \code{value_num},
\code{value_label}.
}
\description{
Use this function when your data already contains a column of days relative
to a reference date (e.g., read from a CSV or built with
\code{\link[tibble:tribble]{tibble::tribble()}}). For Excel files with absolute dates, use
\code{\link[=load_lab_data]{load_lab_data()}} instead.
}
\examples{
raw <- data.frame(
  case_id   = c("Case 1", "Case 1", "Case 2"),
  relday    = c(0, 22, 0),
  parameter = c("Hemoglobin (g/dL)", "Hemoglobin (g/dL)", "WBC (/µL)"),
  value     = c("10.5", "<8", "27300")
)
prep_lab_data(raw)

}
